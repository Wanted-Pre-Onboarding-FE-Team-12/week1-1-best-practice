import React from "react";

import AuthForm from "@components/AuthForm";
import Layout from "layout";

const Auth = () => {
    return (
        <Layout>
            <AuthForm />
        </Layout>
    );
};

export default Auth;
